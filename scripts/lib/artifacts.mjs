/** Shared list of committed install artifacts produced by the build. */
export const ARTIFACT_FILES = Object.freeze([
  'youtube-bot-comments-filter.user.js',
  'youtube-bot-comments-filter.meta.js',
]);
